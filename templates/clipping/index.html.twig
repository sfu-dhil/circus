{% extends 'base.html.twig' %}

{# Clipping Listing Page #}



{% block title %}
    All Items
{%- endblock %}

{% set breadpath =
{ 0:{
'title': 'All Items'
}
}
%}


{% block pageheader %}
    <h1>All Items</h1>
{% endblock %}

{% block action_buttons %}
    {% if is_granted('ROLE_CONTENT_ADMIN') %}
        <a href="{{ path('clipping_new') }}" class="btn btn-secondary">
            <span class="bi bi-plus-lg"></span> New Item
        </a>
    {% endif %}
{% endblock %}



{% block body %}
<!-- Admin Toolbar -->


<article>

    {% include 'clipping/partial/gallery.html.twig' %}

</article>


<!-- Pager -->
<div class="navigation bottom-pager">
    {{ knp_pagination_render(clippings) }}
</div>


            <!-- List Count -->
            {# <div class="listing-count">
                <p>Displaying <strong>{{ clippings|length }}</strong> items of
                    <strong>{{ clippings.getTotalItemCount }}</strong> total.</p>
            </div> #}



{% endblock %}
